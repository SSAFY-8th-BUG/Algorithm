SELECT CATEGORY, SUM(SALES) AS TOTAL_SALES FROM
(SELECT BOOK_SALES.BOOK_ID, BOOK_SALES.SALES_DATE, BOOK_SALES.SALES, BOOK.CATEGORY FROM BOOK_SALES LEFT JOIN BOOK ON BOOK.BOOK_ID = BOOK_SALES.BOOK_ID WHERE MONTH(SALES_DATE)=1 ) B 
GROUP BY CATEGORY ORDER BY CATEGORY;